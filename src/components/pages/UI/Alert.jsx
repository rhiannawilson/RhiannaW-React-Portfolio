// props alert for the ContactForm to ensure the user fills in the message box before submitting the form request
// function Alert(props) {
//     console.log(props);
  
//     return (
//       <div className={`alert alert-${props.type || 'success'}`} role="alert">
//         {props.message}
//       </div>
//     );
//   }
  
//   export default Alert;
  