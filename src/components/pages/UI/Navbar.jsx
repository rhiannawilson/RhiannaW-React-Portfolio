
export default function Nav({ links }) {
    return (
      <nav className="navbar">
            <ul className="navbar">
              <li className="navbar">
                {links.map((link) => link)}
              </li>
            </ul>
      </nav>



    );
  }
  